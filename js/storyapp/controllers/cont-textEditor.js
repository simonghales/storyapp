storyApp.controller('TextEditor', ['$scope', function($scope) {
    var textEditor = this;

    // get data

    textEditor.data = {
        color : "#000000",
        fontSize : "18px",
        textAlign : "left"
    }

    // init colour picker?

}]);